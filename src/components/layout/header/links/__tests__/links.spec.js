//endregion
//region Local Imports
import { LINKS_COMPONENT_KEY } from "~components/layout";
//endregion
describe(LINKS_COMPONENT_KEY, function () {
    it("should render as a " + LINKS_COMPONENT_KEY + " by default", function () {
        var testId = "links";
        // const { getByTestId } = render(<Links data-testid={testId}/>);
        // const container = getByTestId(testId);
    });
});
